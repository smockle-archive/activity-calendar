service: activity-calendar

provider:
  name: aws
  runtime: nodejs6.10

functions:
  app:
    handler: index.handler
    events:
      - http:
          path: ingest
          method: get

plugins:
  - serverless-offline